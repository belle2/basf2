Import('env')

#These are the libraries the awesome package has to be linked against
env['LIBS'] = ['framework','$ROOT_LIBS', 'mdst_dataobjects','beast_plume_dataobjects', 'simulation_dataobjects']

Return('env')
