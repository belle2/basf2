#
# Config object (confname = RUNCONTROL@RC:test:daq:random:limited:10kHz)
#

nodename : RUNCONTROL
config : RC:test:daq:random:limited:10kHz

node : object(node:test:daq:random:limited:10kHz)

#
#
#
