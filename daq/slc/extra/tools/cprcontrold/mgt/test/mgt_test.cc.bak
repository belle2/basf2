#include "mgt.h"

int main (int argc, const char** argv) {
  int firmware, hardware;
  Mgt_t* mgt = mgt_boot(0, argv[1], &firmware, &hardware);
  return 0;
  /*
  mgt_start(mgt);
  mgt_launch(mgt, 4);
  mgt_stop(mgt);
  mgt_pause(mgt);
  */
  mgt_quit(mgt);
}
