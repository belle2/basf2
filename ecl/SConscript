Import('env')

env['LIBS'] = ['framework', 'geometry', 'ecl_dbobjects', 'simulation','ecl_dataobjects', 'mdst_dataobjects',
               'simulation_dataobjects', 'Geom', 'calibration', 'calibration_dataobjects', '$ROOT_LIBS']

Return('env')
