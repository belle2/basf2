Import('env')

env['LIBS'] = ['framework', 'geometry', 'ecl_dbobjects', 'simulation','ecl_dataobjects', 'simulation_dataobjects', 'mdst_dataobjects', 'rawdata_dataobjects',
               '$ROOT_LIBS', 'Geom']

Return('env')
