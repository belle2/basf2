Import('env')

env['LIBS'] = ['framework', 'calibration', 'geometry', 'ecl_dbobjects', 'simulation','ecl_dataobjects', 'mdst_dataobjects',
               '$ROOT_LIBS', 'Geom', 'analysis']

Return('env')
