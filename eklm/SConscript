Import('env')

env['LIBS'] = ['eklm_dataobjects', 'eklm_dbobjects','klm_dataobjects',
               'framework', 'geometry', 'simulation', 'mdst_dataobjects',
               'calibration', 'calibration_dataobjects', '$ROOT_LIBS', 'Geom']

Return('env')
