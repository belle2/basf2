Import('env')

env['LIBS'] = ['framework', 'simulation', '$ROOT_LIBS', 'Geom',
               'G4root', '$GEANT4_LIBS', 'CLHEP',
               'boost_python',
               '$PYTHON_LIBS']

Return('env')
