Import('env')

env['LIBS'] = ['framework', '$ROOT_LIBS', 'Geom',
               'g4root', '$GEANT4_LIBS', 'CLHEP',
               'boost_python', 'boost_iostreams',
               '$PYTHON_LIBS']

Return('env')
