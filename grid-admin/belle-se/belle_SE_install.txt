=Belle SE install=
* setup repos: lcg-CA, ig, glite-generic

cd /etc/yum.repos.d/
wget http://grid-it.cnaf.infn.it/mrepo/repos/glite-generic.repo
wget http://grid-it.cnaf.infn.it/mrepo/repos/lcg-ca.repo
wget http://grid-it.cnaf.infn.it/mrepo/repos/ig.repo

yum install lcg-CA
yum install ig_SE_storm_backend 
yum install ig_SE_storm_frontend
yum install ig_GRIDFTP

#copy host certificates


yum install openvpn
#copy openvpn client.conf to /etc/openvpn and start the service

#update fstab with the mounts

#mount the mounts

#install java1.5 JDK from sun website


#copy site info.def
/opt/glite/yaim/bin/ig_yaim --verify -s /root/siteinfo/site-info.def -n ig_SE_storm_backend -n ig_SE_storm_frontend -n ig_GRIDFTP


UPDATE `storage_space` SET `alias`='BELLE_TOKEN', `total_size`='70866960384', `guar_size`='70866960384', `free_size`='70866960384', `space_file`='/bhsm' WHERE space_token='BB648471-2F01-DE0E-8257-69E8FC5F7794'


