Changes in the mva package since release-11-00
----------------------------------------------

* The importance plot produced by ``basf2_mva_evaluate.py`` now shows absolute importance as the fraction of total importance per method (each column sums to 1), making columns directly comparable across methods regardless of their internal importance scale. Previously the scale was normalised to a fixed 0-100 interval without providing the absolute scale.
