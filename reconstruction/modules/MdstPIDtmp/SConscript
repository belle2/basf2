Import('env')

env['LIBS'] = ['framework', 'reconstruction_dataobjects', 'generators_dataobjects',
               'tracking_dataobjects', 'top_dataobjects', 'arich_dataobjects',
               'genfit', '$ROOT_LIBS']

Return('env')
