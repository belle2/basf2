Import('env')

env['LIBS'] = ['pxd', 'framework', 'geometry', 'simulation', 'genfit',
               'generators', '$ROOT_LIBS', 'Geom', 'EG', 'g4root',
               '$GEANT4_LIBS', 'CLHEP']

Return('env')
