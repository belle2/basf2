Import('env')

env['LIBS'] = ['framework', 'geometry', 'simulation', 'simulation_dataobjects',
		'genfit2', 'generators_dataobjects', '$ROOT_LIBS', 'Geom', 'vxd','vxd_dataobjects',
		'svd_dataobjects' ]

Return('env')
