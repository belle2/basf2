Import('env')

env['LIBS'] = ['framework', 'top_dataobjects', 'generators_dataobjects', 
               'mdst_dataobjects', 'simulation_dataobjects',
               '$ROOT_LIBS', '$GEANT4_LIBS', 'CLHEP']

Return('env')
