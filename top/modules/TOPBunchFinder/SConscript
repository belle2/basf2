Import('env')

env['LIBS'] = ['framework', 'geometry', 'top_dataobjects', 'mdst_dataobjects',
               'tracking_dataobjects', 'top', 'top_reconstruction', '$ROOT_LIBS', 
	       'reconstruction_dataobjects', 'gfortran']

Return('env')
