Import('env')

env['LIBS'] = ['framework', 'top_dataobjects', 'mdst_dataobjects', 
               'tracking_dataobjects', 'top_reconstruction', 
               'top', '$ROOT_LIBS', 'gfortran']

env.Append(CCFLAGS = ['-g'])

Return('env')

