Import('env')

env['LIBS'] = ['framework', 'simulation', 'geometry',
               'tracking_dataobjects', 'bklm_dataobjects', 'eklm_dataobjects', 'bklm', 'ecl',
               '$ROOT_LIBS', '$GEANT4_LIBS', 'CLHEP']

Return('env')
