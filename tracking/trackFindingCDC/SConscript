Import('env')

import os

# Mark this folder as a sub library
env.Dictionary()['SUBLIB'] = True

# Additional libraries
env['LIBS'] = [
    'tracking',
    'tracking_dataobjects',
    'cdc', 'cdc_dataobjects',
    'mdst_dataobjects',
    'framework',
    'framework_pybasf2',
    'genfit2',
    '$ROOT_LIBS',
    ]

Return('env')
