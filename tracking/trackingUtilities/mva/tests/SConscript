Import('env')

# Use the common libraries of the track finder for the test compilation
env['LIBS'] = [
    'tracking_trackingUtilities',
    'framework',
    'boost_python',
    '$ROOT_LIBS',
    '$PYTHON_LIBS',
    ]

Return('env')
