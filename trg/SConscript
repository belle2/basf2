Import('env')

#env['LIBS'] = ['trg']
#env.Append(CPPDEFINES = {'CDCTRIGGER_DEBUG':None,
#                         'CDCTRIGGER_DISPLAY':None})
#env.ParseConfig('pkg-config --cflags --libs gtkmm-2.4')
#print env.Dump()

Return('env')
