Import('env')

# env['CONTINUE'] = False

env['LIBS'] = ['framework',
	       'cdc',
	       'ecl',
	       'rawdata_dataobjects',
	       'cdc_dataobjects',
	       'ecl_dataobjects',
	       'trg_ecl_dataobjects']
#env.Append(CPPDEFINES = {'TRG_DEBUG':None})
#env.Append(CPPDEFINES = {'TRG_DISPLAY':None})
#env.Append(CPPDEFINES = {'TRGCDC_DISPLAY':None})
#env.Append(CPPDEFINES = {'TRGCDC_DISPLAY_HOUGH':None})
#env.ParseConfig('pkg-config --cflags --libs gtkmm-2.4')
#print env.Dump()

Return('env')

